# B028 Runtime Unification

B028 stops adding foundational kernels and introduces a single browser-side integration adapter over the proven B019–B027 contracts.

The client now has explicit Avatar / Architect / God modes, a unified event/history surface, settlement registry, DNA inspection/edit adapters and timeline forking.

Truth boundary: this bundle does **not** claim that every prior headless simulation is already executing live inside the canvas. B029 must replace adapters with real canvas-bound simulation modules one domain at a time.
