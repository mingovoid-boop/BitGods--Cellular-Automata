# B021 Emergent Settlement Runtime

B021 verifies that settlement status is detected from agent behavior and persistent state rather than spawned directly.

Causal path:

Need → discovery → memory → knowledge sharing → convergence → shared storage → shelters → farming → specialization → settlement detection.

The verified deterministic run uses 24 agents. The browser adapter is present, but full visual binding remains a later gate.
